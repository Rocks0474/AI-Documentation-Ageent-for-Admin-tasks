"""LangGraph orchestration package.

The orchestration graph (``graph.py``) is built in Phase 3, after all adapters,
schemas, agents, and the ER compliance module are in place. This package is
present now so the project packaging and imports resolve cleanly.
"""
